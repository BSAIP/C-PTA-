#include <stdio.h>
int main() 
{
    double x,cost;
    scanf("%lf",&x);
    if(x<0)
    printf("Invalid Value!");
    else
    {
        if(x<=50)
        cost=0.53*x;
        else
        cost=0.53*50+(x-50)*0.58;
        printf("cost = %.2f",cost);
    }
}

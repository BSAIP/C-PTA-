#include <stdio.h>
int main()
{
    int n,i=0;
    scanf("%d",&n);
    do{ if(2*i+1==n||2*i+1==n-1)
        printf("%d",2*i+1);
        else
        printf("%d ",2*i+1);
        i++;
    }while(2*i+1<n+1);
}

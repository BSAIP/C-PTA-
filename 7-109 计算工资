#include <stdio.h>
int main()
{
    int year,hour,t;
    double salary;
    scanf("%d %d",&year,&hour);
    if(year>=5)
    t=50;
    else
    t=30;
    if(hour<=40)
    salary=t*hour;
    else
    salary=40*t+1.5*t*(hour-40);
    printf("%.2f",salary);
}



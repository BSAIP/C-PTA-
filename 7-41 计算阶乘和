#include<stdio.h>
int main()
{
    int S=0,N,a,i;
    scanf("%d",&N);
    while(N>0)
    {
        a=1;i=N;
        while(i>0)
        {
            a*=i;
            i--;
        }
        N--;
        S+=a;
    }
    printf("%d",S);
    
}

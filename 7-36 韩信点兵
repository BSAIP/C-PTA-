#include <stdio.h>
int main() 
{   
    int n=21,flag=0;
    for(;;)
    {
        if(n%5==1&&n%6==5&&n%7==4&&n%11==10)
        {flag=1;}
        if(flag==1)
        break;
        n++;
    }
    printf("%d",n);
}

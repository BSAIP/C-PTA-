#include <stdio.h>
int main()
{
 int n,i=1;
 scanf("%d",&n);
 do{if(i==1)
 printf("%d",i);
 else 
  printf(" %d",i);
  i++;
 }while(i<n);
 if(n!=1)
 {
 do{
  printf(" %d",n);
  n--;  
  }while(n>0);
 }
 
}

#include <stdio.h>
#include <math.h>
int main() 
{
    int N;
    double sum=1.0,i=1.0;
    scanf("%d",&N);
    while(i<N)
    {
        i++;
        sum=sum+pow(-1,i+1)*i/(2*i-1);
    }
    printf("%.3f",sum);
    return 0;
}

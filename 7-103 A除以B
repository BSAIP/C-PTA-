#include <stdio.h>
int main()
{
    int A,B;
    scanf("%d %d",&A,&B);
    if(B>0)
    printf("%d/%d=%.2f",A,B,A*1.0/B);
    else if(B<0)
    printf("%d/(%d)=%.2f",A,B,A*1.0/B);
    else
    printf("%d/%d=Error",A,B);
    return 0;
}
